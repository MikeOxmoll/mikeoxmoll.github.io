import React from 'react';

function useVideoReducer(component: React.FC) {
    // return function WrappedComponent() {
    //     const myHookValue = useMyHook();
    //     return <Component {...props} myHookValue={myHookValue} />;
    // }
}